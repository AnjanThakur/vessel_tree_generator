# Submission demonstration cases

These four cases share one seeded anatomy and differ only by the explicit disease configuration. Open each case's `vtk/cine.pvd` in ParaView. See `demo_summary.json` for validation status.
